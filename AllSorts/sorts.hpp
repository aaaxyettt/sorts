#ifndef SORT_HPP
#define SORT_HPP
#include <iostream>
#include <time.h>

void random(int*, int);

void print(int*, int);

void swap(int&, int&);

int search(int*, int);

bool check(int*, int);

void NewNumber(int*, int, int);

void printBigAr(int*, int);

void printSmallAr(int*);

void bubbleSort(int*, int);

void selectionSort(int*, int);

void insertionSort(int*, int);

void countSort(int*, int*, int);

void randomFill(int*, int, int, int);

#endif // SORT_HPP
